# alx-low_level_programming
# malloc free projects

This project includes:
	* a function that creates an array of chars
	* a function that returns a pointer to a newly allocated space
	* a function that concatenates two strings.
	* a function that returns a pointer to a 2 dimensional array
	* a function that frees a 2 dimensional grid
	* a function tha concatenates all the arguments of a program
